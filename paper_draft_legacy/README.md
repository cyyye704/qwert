# 旧论文草稿说明

这里的 `report.md` 和 `chapters/` 属于旧 Scheme B 论文链，保留是为了迁移后参考结构和文字素材。

它们的 Q2、Q3、Q4 数字和模型口径与当前 c-spec 结果不一致，不得直接作为最终论文继续发布。正式重写应以迁移包根目录的 `summary.json`、`results/summary.md`、`results/problem*.json/csv` 和 `figures/` 为事实来源。
